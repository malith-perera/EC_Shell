#include <stdio.h>
#include "ec.h"

int main (int argc, char *argv[])
{
  /* print version number   if (argc < 2) {
    // report version
    printf("%s Version %s.%s\n", argv[0], EC_VERSION_MAJOR, EC_VERSION_MINOR);
    printf("Usage %s nubmer\n", argv[0]);
    return 1;
  }
  */

  return 0;
}
